# Copy this file to ".env" (same folder as package.json) if you need to
# change where the contact form sends its requests — e.g. once you
# deploy the backend in ../backend somewhere other than localhost.

# Vite only exposes env vars to the browser if they start with VITE_.

VITE_CONTACT_API_URL=http://localhost:5000/api/contact
